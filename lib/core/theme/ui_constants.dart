/// UI layout constants for the HoopAnalytics design system.
///
/// Touch targets follow Material accessibility guidelines (≥ 48dp), while the
/// Court View action buttons use a larger 56dp target for fast annotation.
/// See Plan.md T-004 and Agent_Mobile.md §12.
library;

/// Minimum interactive touch target (Material a11y guideline).
const double kMinTouchTarget = 48.0;

/// Court View primary action button size.
const double kActionButtonSize = 56.0;

/// Spacing scale.
const double kSpacingXS = 4.0;
const double kSpacingS = 8.0;
const double kSpacingM = 16.0;
const double kSpacingL = 24.0;
const double kSpacingXL = 32.0;
